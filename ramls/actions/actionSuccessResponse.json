{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Action response",
  "type": "object",
  "properties": {
    "accountId": {
      "type": "string",
      "description": "Unique ID of the account",
      "$ref": "../raml-util/schemas/uuid.schema"
    },
    "amount": {
      "type": "string",
      "description": "Action amount"
    },
    "feeFineActionIds": {
      "type": "array",
      "description": "IDs of fee/fine action records created as a result of the action call",
      "items": {
        "type": "string",
        "$ref": "../raml-util/schemas/uuid.schema"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "accountId",
    "amount"
  ]
}
